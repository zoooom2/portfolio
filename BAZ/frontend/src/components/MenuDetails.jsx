import styled from 'styled-components';

const MenuDetails = () => {
  return <Wrapper>Menu Details</Wrapper>;
};

const Wrapper = styled.div`
  border-left: 1px solid rgba(0, 0, 0, 0.2);
  margin-block: 1em;
  padding: 0.5em 1rem;
`;

export default MenuDetails;
